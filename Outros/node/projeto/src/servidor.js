function findOdd(A) {
  
  for (number of A){
    // console.log(A.filter(n => n === number).length % 2 == true)
    if(A.filter(n => n === number).length % 2 ){
      
      console.log(A.filter(n => n === number).length % 2 == 1)
      return number
    }
  }
}

let x = [1,2,2,3,3,3,4,3,3,3,2,2,1]
console.log(findOdd(x))
